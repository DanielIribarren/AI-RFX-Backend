# AI-RFX Environment Configuration Template
# Copy this file to .env and update with your actual values

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
ENVIRONMENT=development
DEBUG=true

# =============================================================================
# DATABASE CONFIGURATION (Supabase)
# =============================================================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# AI CONFIGURATION - 🚀 OPTIMIZED FOR GPT-4o
# =============================================================================
OPENAI_API_KEY=sk-your-openai-api-key-here
# 🚀 UPGRADED: GPT-4o with 128k context window (vs gpt-3.5-turbo 16k)
OPENAI_MODEL=gpt-4o
# 🚀 UPGRADED: 4096 token responses (vs 1500) for more detailed extractions  
OPENAI_MAX_TOKENS=4096
OPENAI_TEMPERATURE=0.1
# 🚀 UPGRADED: 60s timeout (vs 30s) for complex document processing
OPENAI_TIMEOUT=60
# 🚀 NEW: Context window size for intelligent chunking
OPENAI_CONTEXT_WINDOW=128000

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
HOST=localhost
PORT=8000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# =============================================================================
# FILE UPLOAD CONFIGURATION
# =============================================================================
MAX_FILE_SIZE_MB=16
MAX_TOTAL_SIZE_MB=32
ALLOWED_EXTENSIONS=pdf,doc,docx,txt,xlsx,csv,png,jpg,jpeg,tiff,zip
UPLOAD_FOLDER=uploads

# =============================================================================
# MULTI-FILE PROCESSING FEATURE FLAGS
# =============================================================================

# Enable OCR processing for scanned PDFs and images
# When true: Uses pytesseract and pdf2image for text extraction from images/scanned docs
# When false: Skips OCR processing, may fail on scanned documents
RFX_USE_OCR=true

# Enable ZIP file expansion and processing
# When true: Automatically extracts and processes files within ZIP archives
# When false: Treats ZIP files as regular binary files (ignored)
RFX_USE_ZIP=true

# =============================================================================
# FEATURE FLAGS - AI AGENT IMPROVEMENTS
# =============================================================================

# Enable evaluation system for AI responses
# When true: Activates automatic evaluation, metrics collection, A/B testing
ENABLE_EVALS=false

# Enable meta-prompting capabilities  
# When true: Activates AI self-improvement cycles, response optimization
ENABLE_META_PROMPTING=false

# Enable vertical agent specialization
# When true: Activates domain-specific optimizations, catering industry focus
ENABLE_VERTICAL_AGENT=false

# =============================================================================
# DEBUG FLAGS
# =============================================================================

# Enable detailed evaluation debugging
# When true: Provides step-by-step evaluation traces, detailed logging
EVAL_DEBUG_MODE=false

# =============================================================================
# PRODUCTION NOTES
# =============================================================================
# For production deployment:
# 1. Set ENVIRONMENT=production
# 2. Set DEBUG=false  
# 3. Use strong secrets for all API keys
# 4. Enable feature flags gradually: start with ENABLE_EVALS=true
# 5. Monitor logs when enabling EVAL_DEBUG_MODE=true
#
# Feature Flag Rollout Strategy:
# Stage 1: ENABLE_EVALS=true (basic evaluation)
# Stage 2: ENABLE_VERTICAL_AGENT=true (domain specialization)  
# Stage 3: ENABLE_META_PROMPTING=true (self-improvement)
#
# Debug flags should only be enabled in development/staging environments.
