# ===========================================
# AI-RFX Backend - Railway Optimized Requirements
# ===========================================
# Requisitos optimizados específicamente para Railway
# Solo dependencias esenciales para funcionamiento core

# Core Web Framework (Esencial)
Flask==3.0.0
flask-cors==4.0.0
Werkzeug==3.0.1

# Production Server (Esencial para Railway)
gunicorn==21.2.0

# Environment & Configuration (Esencial)
python-dotenv==1.0.0

# Data Validation & Models (Esencial)
pydantic==2.5.2

# AI & OpenAI Integration (Core business logic)
openai==1.7.2

# Database Integration (Esencial)
supabase==2.6.0

# Document Processing (Esencial - solo básico)
PyPDF2==3.0.1
python-docx==1.1.0

# Template Engine (Esencial)
Jinja2==3.1.2
MarkupSafe==2.1.3

# Date & Time Processing (Esencial)
python-dateutil==2.8.2

# HTTP Requests (Esencial)
requests==2.31.0
urllib3==2.1.0

# Utilities (Esencial)
click==8.1.7
